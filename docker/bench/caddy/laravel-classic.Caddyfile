# laravel-classic (`make benchmark-http`): one PHP thread per request in flight, the application bootstrapped for each.
{
	auto_https off
	admin off
	frankenphp {
		num_threads 8
	}
}

:8080 {
	root * /app/public
	php_server
}
