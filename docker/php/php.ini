; Settings for OZone's tests and tools.

memory_limit = 1G
error_reporting = E_ALL
display_errors = On
date.timezone = UTC

; pcov is loaded but idle: `make test-coverage` turns it on.
pcov.enabled = 0
pcov.directory = /ozone/oz
