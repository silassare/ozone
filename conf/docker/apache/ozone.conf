# Lets the scope's .htaccess (public/<scope>/.htaccess, see conf/apache/) route requests to
# index.php, and hides the Apache version.

ServerTokens Prod
ServerSignature Off

<Directory /var/www/html/public>
	Options -Indexes +FollowSymLinks
	AllowOverride All
	Require all granted
</Directory>
